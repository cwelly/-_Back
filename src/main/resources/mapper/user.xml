<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.passproject.repository.UserRepository">

	<insert id = "save" parameterType="user" >
		insert into user(email_id, email_domain, password, name, addr, phone) values (#{email.id},#{email.domain},#{password},#{name},#{addr},#{phone})
	</insert>
	
	<select id="findByEmail" parameterType="email" resultType="user">
		select * 
		from user 
		where email_id=#{id} and email_domain=#{domain}
	</select>
	
	<select id="login" parameterType="map" resultType="user" >
		select * 
		from user 
		where email_id=#{emailid} and email_domain=#{emaildomain} and password=#{userpassword}
	</select>
	
	<delete id="delete" parameterType="email" >
		delete 
		from user 
		where email_id=#{user.email.id} and email_domain=#{user.email.domain}
	</delete>
	
	<update id="update" parameterType="user">
		update user
		<trim prefix="set" suffixOverrides=",">
			<if test="password != null">password = #{user.password},</if>
			<if test="name != null">name = #{user.name},</if>
			<if test="addr != null">addr = #{user.addr},</if>
			<if test="phone != null">phone = #{user.phone},</if>
		</trim>
		where email_id=#{user.email.id} and email_domain=#{user.email.domain}
	</update>
	
	
	<insert id="saveInterestRegion" parameterType="map">
		insert 
		into interest_region values (#{userno}, #{dongcode})
	</insert>
	
</mapper>